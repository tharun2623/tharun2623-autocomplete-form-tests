{
  "name": "autocomplete-form-tests",
  "version": "1.0.0",
  "description": "QA test suite for the Autocomplete Form feature",
  "scripts": {
    "test": "npm run test:ui && npm run test:api",
    "test:ui": "npx playwright test --config=tests/ui/config/playwright.config.ts",
    "test:ui:headed": "npx playwright test --config=tests/ui/config/playwright.config.ts --headed",
    "test:ui:report": "npx playwright test --config=tests/ui/config/playwright.config.ts && npx playwright show-report",
    "test:api": "npx jest --config=jest.config.js",
    "test:api:verbose": "npx jest --config=jest.config.js --verbose"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.12",
    "axios": "^1.7.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.5"
  }
}
